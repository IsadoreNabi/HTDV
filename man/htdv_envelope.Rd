% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{htdv_envelope}
\alias{htdv_envelope}
\title{Berger-Robust Envelope Posterior}
\usage{
htdv_envelope(fits, target = "theta")
}
\arguments{
\item{fits}{List of \code{htdv_fit} objects on the same data.}

\item{target}{Name of the Stan parameter over which to build the envelope.}
}
\value{
A list with \code{draws} (envelope draws for the target parameter),
  \code{weights}, \code{intervals}, and \code{component_intervals}.
}
\description{
Builds an envelope posterior over a list of \code{htdv_fit} objects by
entropy-maximizing mixture weights subject to predictive consistency
(Berger 1994).
}
\examples{
\donttest{
x <- rnorm(50)
f1 <- htdv_fit(x, model = "tac", chains = 2, iter = 500,
               refresh = 0, seed = 1)
f2 <- htdv_fit(x, model = "wsc", chains = 2, iter = 500,
               refresh = 0, seed = 2)
htdv_envelope(list(f1, f2), target = "theta")$intervals
}

}
\references{
Berger, J.O. (1994). An Overview of Robust Bayesian Analysis. Test 3(1):
5-124.
}
